Algoritmo "RepeticaoPara"

Var

   n , i , x , soma : inteiro


Inicio

      escreval("Quantos numero serao digitado : ")
      leia(n)
      
      soma <- 0
      para i de 1 ate n faca
         escreval("Digite um numero :")
         leia (x)
         soma <- soma + x
      fimpara
      escreval("Soma =",soma)

Fimalgoritmo